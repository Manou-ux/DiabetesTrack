name: diabetes_track
description: "Suivi du Diabete: glycemie, medicament et alimentation"
publish_to: 'none'
version: 1.0.0+1

# Bloc manquant à ajouter :
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive_flutter: ^1.1.0
  fl_chart: ^0.66.0
  intl: ^0.19.0
  google_fonts: ^6.2.1  

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.8

flutter:
  uses-material-design: true